<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Product - Admin</title>
    <link rel="icon" type="image/svg+xml" href="/favicon.svg">
    <link rel="stylesheet" href="/css/style.css">
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600&display=swap" rel="stylesheet">
</head>
<body>
    <div class="admin-container">
        <div class="admin-header">
            <h1>Edit Product</h1>
            <a href="/dashboard" class="back-btn">← Back to Dashboard</a>
        </div>

        <% if (typeof success !== 'undefined' && success) { %>
            <div class="success-message">
                Product updated successfully!
            </div>
        <% } %>

        <% if (typeof error !== 'undefined' && error) { %>
            <div class="error-message">
                <%= error %>
            </div>
        <% } %>

        <% if (product) { %>
            <form class="edit-product-form" id="editProductForm" method="POST" action="/admin/products/<%= product.id %>" enctype="multipart/form-data">
                <div class="form-row">
                    <div class="form-group">
                        <label for="name">Product Name</label>
                        <input type="text" id="name" name="name" value="<%= product.name %>" required>
                    </div>
                    <div class="form-group">
                        <label for="price">Price (€)</label>
                        <input type="number" id="price" name="price" value="<%= product.price %>" step="0.01" min="0" required>
                    </div>
                </div>

                <div class="form-group">
                    <label for="imageFiles">Upload Images (Multiple)</label>
                    <input type="file" id="imageFiles" name="imageFiles" accept="image/*" multiple onchange="previewImages(this)">
                    <div class="form-help">Supported formats: JPG, PNG, GIF, WebP (Max 5MB each). You can select multiple files. The first image will be the main product image.</div>
                    <div class="images-preview" id="imagesPreview" class="hidden">
                        <div class="preview-grid" id="previewGrid"></div>
                    </div>
                    <% if (product.image) { %>
                        <div class="current-image">
                            <p><strong>Current main image:</strong></p>
                            <img src="<%= product.image %>" alt="Current product image" class="image-preview">
                        </div>
                    <% } %>
                </div>

                <div class="form-group">
                    <label for="description">Description</label>
                    <textarea id="description" name="description" required placeholder="Describe the product..."><%= product.description %></textarea>
                </div>

                <div class="form-group">
                    <label for="productDetails">Product Details</label>
                    <textarea id="productDetails" name="productDetails" rows="4" placeholder="Enter product details (e.g., Handcrafted ceramic, Food safe glaze, Dishwasher safe, Unique piece - slight variations may occur)"><%= product.productDetails || '' %></textarea>
                </div>
                
                <div class="form-actions">
                    <button type="submit" class="btn-save">Update Product</button>
                    <button type="button" class="btn-cancel" id="cancelBtn">Cancel</button>
                </div>
            </form>
        <% } else { %>
            <div class="error-message">
                Product not found or failed to load.
            </div>
        <% } %>
    </div>

    <script src="/js/admin.js"></script>
</body>
</html>
