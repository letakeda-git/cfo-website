<!DOCTYPE html>
<html lang="pt">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= product.name %> - Agatha Oeiras</title>
    <link rel="icon" type="image/svg+xml" href="/favicon.svg">
    <link rel="stylesheet" href="/css/style.css">
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600&display=swap" rel="stylesheet">
</head>
<body>
    <nav class="navbar">
        <div class="nav-container">
            <a href="/" class="nav-logo">Agatha Oeiras</a>
            <ul class="nav-menu">
                <li><a href="/" class="nav-link">Início</a></li>
                <li><a href="/products" class="nav-link">Produtos</a></li>
                <li><a href="/about" class="nav-link">Sobre</a></li>
                <li><a href="/contact" class="nav-link">Contacto</a></li>
            </ul>
            <div class="nav-cart">
                <a href="/cart" class="cart-link">
                    <svg class="cart-icon" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round">
                        <path d="M3 3h2l.4 2M7 13h10l4-8H5.4m0 0L7 13m0 0l-2.5 5M7 13l2.5 5m0 0h8.5M9.5 18a1.5 1.5 0 1 0 0 3 1.5 1.5 0 0 0 0-3zm8.5 0a1.5 1.5 0 1 0 0 3 1.5 1.5 0 0 0 0-3z"/>
                    </svg>
                    <span class="cart-count" id="cart-count"><%= cartItemCount || 0 %></span>
                </a>
            </div>
            <div class="hamburger">
                <span class="bar"></span>
                <span class="bar"></span>
                <span class="bar"></span>
            </div>
        </div>
    </nav>

    <main>
        <section class="product-detail">
            <div class="container">
                <div class="product-detail-content">
                    <div class="product-images">
                        <div class="main-image-container">
                            <img id="mainImage" src="<%= product.image %>" alt="<%= product.name %>">
                            <% if (product.images && product.images.length > 1) { %>
                            <div class="image-navigation">
                                <button class="nav-btn prev-btn" id="prevBtn">‹</button>
                                <button class="nav-btn next-btn" id="nextBtn">›</button>
                            </div>
                            <div class="image-counter">
                                <span id="currentImageNumber">1</span> / <span id="totalImages"><%= product.images.length %></span>
                            </div>
                            <% } %>
                        </div>
                    </div>
                    <div class="product-details">
                        <h1><%= product.name %></h1>
                        <p class="product-price">€<%= product.price %></p>
                        <p class="product-description"><%= product.description %></p>
                        
                        <div class="product-actions">
                            <button class="btn btn-primary btn-large add-to-cart-btn" data-product-id="<%= product.id %>" data-product-name="<%= product.name %>" data-product-price="<%= product.price %>">Adicionar ao Carrinho</button>
                            <button class="btn btn-outline btn-large">Adicionar à Lista de Desejos</button>
                        </div>
                        
                        <% if (product.productDetails) { %>
                        <div class="product-info">
                            <h3>Detalhes do Produto</h3>
                            <div class="product-details-content">
                                <%= product.productDetails.replace(/\n/g, '<br>') %>
                            </div>
                        </div>
                        <% } %>
                    </div>
                </div>
            </div>
        </section>

    </main>

    <footer class="footer">
        <div class="footer-container">
            <div class="footer-content">
                <div class="footer-section">
                    <h3>Agatha Oeiras</h3>
                    <p>Cerâmica artesanal para vida moderna</p>
                </div>
                <div class="footer-section">
                    <h4>Links Rápidos</h4>
                    <ul>
                        <li><a href="/">Início</a></li>
                        <li><a href="/products">Produtos</a></li>
                        <li><a href="/about">Sobre</a></li>
                        <li><a href="/contact">Contacto</a></li>
                    </ul>
                </div>
                <div class="footer-section">
                    <h4>Contacto</h4>
                    <p>agatha.abdala@hotmail.com</p>
                    <p>+351 967 799 032</p>
                </div>
            </div>
            <div class="footer-bottom">
                <p>&copy; 2024 Agatha Oeiras. Todos os direitos reservados. | <a href="/admin/login" class="admin-footer-link">Admin</a></p>
            </div>
        </div>
    </footer>

    <script src="/js/script.js"></script>
    <script src="/js/product.js"></script>
    <% if (product.images && product.images.length > 1) { %>
    <script id="productImagesData" type="application/json"><%- JSON.stringify(product.images) %></script>
    <% } %>
</body>
</html>
